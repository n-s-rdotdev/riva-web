# Session Notes

## Reference Project

When asked to refer to "the other project", use:
`/Users/nrakshit/Documents/projects/experiments/riva`

## Changes Discussed And Made

### Protected Layout Shell

- Created a protected app shell for `src/app/(protected)`.
- Added a header and sidebar using shadcn components.
- Navigation routes included:
  - Dashboard
  - Transactions
  - Spaces
  - Sources
  - Calendar

### Reference UI Restructure

- Reworked the shell to match the reference project structure:
  - `site-header`
  - `site-sidebar`
  - `nav-main`
  - `nav-user`
  - `site-command`
- Added `src/lib/site-config.ts`.
- Added `public/logo.svg` from the reference project.
- Adjusted sidebar item spacing with `gap-2`.

### Schema Restore

- Compared schemas from the reference project.
- Restored matching DB schema structure into `riva-web`.
- Current schema count:
  - Reference project: 12 `pgTable` schemas.
  - `riva-web`: 12 `pgTable` schemas.
- Missing schemas: none.

## Verification

- Ran TypeScript checks with `pnpm exec tsc --noEmit`.
- Ran focused ESLint checks on changed files.
- Browser-tested protected shell navigation and command palette.

## Known Existing Issues

Full `pnpm lint` still fails on unrelated pre-existing issues:

- `src/components/ui/carousel.tsx`
- `src/hooks/use-mobile.ts`
- unused imports in `src/app/(marketting)/page.tsx`
